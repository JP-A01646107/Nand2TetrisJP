/**
 * Not gate:
 * if (in) out = 0, else out = 1
 * Nand(a, b) = Not(a And b)
 * Not(1 and 1 = true) = False
 * Not(0 and 0 = False) = True
 */
CHIP Not {
    IN in;
    OUT out;

    PARTS:
    //// Replace this comment with your code.
    Nand(a=in, b=in, out=out);
}